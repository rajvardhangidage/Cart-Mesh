CREATE TABLE products (id varchar(36) PRIMARY KEY, vendor_id varchar(36) NOT NULL, name varchar(120) NOT NULL, category varchar(120) NOT NULL, sku varchar(50) NOT NULL UNIQUE, description varchar(2000), price decimal(14,2) NOT NULL, active boolean NOT NULL DEFAULT true, created_at datetime(6) NOT NULL); CREATE INDEX idx_product_vendor ON products(vendor_id);